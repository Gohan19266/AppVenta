<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="learn.mode.appventa">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar">
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".activity.main.VentaViewActivity" />
        <activity android:name=".activity.editor.CategoriaActivity" />
        <activity android:name=".activity.editor.ProductoActivity" />
        <activity android:name=".activity.main.ProductoViewActivity" />
        <activity android:name=".activity.main.CategoriaViewActivity" />
        <activity android:name=".activity.main.ClientViewActivity" />
        <activity android:name=".activity.editor.PrincipalActivity" />
        <activity android:name=".activity.editor.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.editor.ClienteActivity"
            android:launchMode="singleTask"
            android:theme="@style/ClienteTheme" />
    </application>

</manifest>